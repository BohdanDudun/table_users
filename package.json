{
  "name": "angularusers",
  "version": "1.0.0",
  "description": "table for edit and delete users",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index",
    "server:dev": "nodemon index",
    "server:prod": "node index",
    "client-install": "npm install --prefix client",
    "client": "npm run start --prefix client",
    "client-build": "npm run build --prefix client",
    "dev": "concurrently \"npm run server:dev\" \"npm run client\""
  },
  "keywords": [
    "express",
    "angular"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.0",
    "mongodb": "^3.2.5",
    "mongoose": "^5.5.10"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.19.0"
  },
  "repository": {
    "type": "git",
    "url": "git@git.evennode.com:804cc233ad726a7b7329eb21d5dfc22f.git"
  }
}
